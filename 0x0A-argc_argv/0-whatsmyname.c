#include <stdio.h>

/**
 * main - the entry point
 * @argc: count
 * @argv: the array
 * Return: Success
 */

int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}

