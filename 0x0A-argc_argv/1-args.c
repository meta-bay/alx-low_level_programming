#include <stdio.h>

/**
 * main - entry point
 * @argc: counter
 * @argv: the array
 * Return: success
 */

int main(int argc, __attribute__((unused)) char *argv[])
{
	printf("%d\n", argc - 1);
	return (0);
}

