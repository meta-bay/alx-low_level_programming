#include "main.h"

/**
 * create_array - pointer to the array
 * @size: the size
 * @c: character
 * Return: idk
 */

char *create_array(unsigned int size, char c)
{
	if (size == 0 )
	{
		return ('\0');
	}
	return (create_array());
}

